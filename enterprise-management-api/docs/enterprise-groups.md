# Enterprise Groups

Enterprise group management uses `GET/PUT /enterprises/enterprise_groups/{enterprise_group_id}`. Request and response
models reference the enterprise group schemas inside `schemas/Enterprise.yaml`.
