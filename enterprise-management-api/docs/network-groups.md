# Network Groups

`GET /enterprises/network-groups` lists network groups available to the caller. Response models are defined under `NetworkGroupsRead` and `NetworkGroupRead` in `schemas/Enterprise.yaml`.
